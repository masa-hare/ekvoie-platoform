export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

/**
 * Returns the admin login path.
 * Replaced Manus OAuth URL — no external dependency required.
 */
export const getLoginUrl = () => "/admin/login";
